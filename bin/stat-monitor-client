#!/usr/bin/ruby

require 'statmonitor/client'

config = StatMonitor::Config.new('/etc/stat-monitor-client/stat-monitor-client.rc')

client = StatMonitor::Client.new(config)

#To do: switch on existence of snapshot test mode?
#client.daemonize()

client.run()

